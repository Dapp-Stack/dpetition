import * as ethers from 'ethers';

export default class PetitionService {
  constructor() {
    process.env.JSON
  }

  public byNetwork(network: string) {

  }

  public findByNetworkAndId(network: string, id: string) {
  }
}
